export const Title = ({text}) => {
  return (
    <header className="py-20 bg-[#1e3932]">
        <div className="px-155">
            <span className="title text-[50px] font-bold text-white">{text}</span>
        </div>
    </header>
  )
}